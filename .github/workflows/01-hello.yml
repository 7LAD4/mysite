name: Hello Action

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-lastest

    steps:
      - name: Say hello~
        run: echo "Hello Github Actions!"